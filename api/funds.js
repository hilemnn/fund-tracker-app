module.exports = function(req, res) { res.setHeader('Access-Control-Allow-Origin', '*'); res.setHeader('Access-Control-Allow-Methods', 'GET, POST, PUT, DELETE, OPTIONS'); res.setHeader('Access-Control-Allow-Headers', 'Content-Type'); if (req.method === 'OPTIONS') { return res.status(200).end(); } if (req.method === 'GET') { const funds = [{ _id: '1', name: 'Fund A', type: 'Equity', price: 100, totalAmount: 5000, payableAmount: 4500 }, { _id: '2', name: 'Fund B', type: 'Bond', price: 95, totalAmount: 3000, payableAmount: 2800 }]; res.status(200).json(funds); } else { res.status(405).json({ error: 'Method not allowed' }); } };
